syntax = "proto3";

package proto;

service MailService {
    rpc SendMail (MailRequest) returns (MailReply) {}
}

message MailRequest {
    int64 userId = 1;
    repeated int64 productsId = 2;

}

message MailReply {
    string response = 1;
}
